syntax = "proto3";

package todoPackage;

message CreateTodoDto {
    string text = 1;
}

message TodoDto {
    int32 id = 1;
    string text = 2;
}

service TodoService {
    rpc createTodo(CreateTodoDto) returns (TodoDto);
}
